/**
 * 심사 룰 엔진.
 *
 * <p>8단계 파이프라인으로 청구를 판정한다:
 * 접수검증 → 계약자격 → 보장판정 → 금액산출 → 한도적용 → 중복·비례 → 이상징후 → 라우팅
 *
 * <p>모든 룰은 순수 함수다. I/O는 파이프라인 진입 전에
 * {@code AdjudicationContext}로 전부 적재되므로, 심사 전체가 인메모리 계산이 된다.
 * 덕분에 모킹 없이 테스트할 수 있고, 같은 입력이면 항상 같은 결과가 나온다.
 *
 * <p>애매하면 부지급이 아니라 회부한다. 자동 부지급의 오탐은 민원·분쟁으로 직결되고,
 * 사람에게 넘기는 비용이 훨씬 싸다.
 *
 * <p>Phase 3에서 구현. 명세: {@code docs/design/03-adjudication.md}
 */
package com.insurance.claims.rules;
